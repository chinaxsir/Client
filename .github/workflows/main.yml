name: Build IPA for TrollStore

on:
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      flutter_version:
        description: 'Flutter SDK version'
        required: false
        default: '3.29.0'

env:
  APP_NAME: XSOPForum
  BUNDLE_ID: com.xsop.forum

jobs:
  build-ipa:
    name: Build TrollStore IPA
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ========== 1. 设置 Flutter ==========
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          version: ${{ github.event.inputs.flutter_version || '3.29.0' }}
          cache: true

      - name: Verify Flutter
        run: flutter config --no-analytics && flutter doctor -v

      # ========== 2. 生成 iOS 平台文件（保护 lib/）==========
      - name: Generate iOS platform
        run: |
          cp -r lib lib_backup
          flutter create . --platforms=ios --project-name=xsop_forum
          rm -rf lib
          mv lib_backup lib
          flutter pub get

      # ========== 3. 安装 CocoaPods ==========
      - name: Install pods
        working-directory: ios
        run: |
          rm -rf Pods Podfile.lock
          pod install 2>&1
          echo "=== Pod install done ==="

      # ========== 4. 写入 Runner.entitlements ==========
      - name: Write Runner.entitlements
        run: |
          cat > ios/Runner/Runner.entitlements <<'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>com.apple.developer.ubiquity-kvstore-access</key>
              <true/>
              <key>com.apple.developer.kernel.extended-virtual-address-space</key>
              <true/>
              <key>com.apple.security.allow-unsigned-executable-memory</key>
              <true/>
              <key>com.apple.developer.playable-content</key>
              <true/>
              <key>com.apple.developer.networking.wifi-info</key>
              <true/>
              <key>com.apple.developer.usb.host-device</key>
              <true/>
              <key>com.apple.developer.avfoundation.audio-fetch</key>
              <true/>
              <key>com.apple.security.files.user-selected.read-only</key>
              <true/>
              <key>com.apple.security.files.user-selected.read-write</key>
              <true/>
          </dict>
          </plist>
          PLIST

      # ========== 5. 写入 Info.plist ==========
      - name: Write Info.plist
        run: |
          cat > ios/Runner/Info.plist <<'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>CFBundleDevelopmentRegion</key>
              <string>zh_CN</string>
              <key>CFBundleDisplayName</key>
              <string>XSOP 论坛</string>
              <key>CFBundleExecutable</key>
              <string>$(EXECUTABLE_NAME)</string>
              <key>CFBundleIdentifier</key>
              <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
              <key>CFBundleInfoDictorVersion</key>
              <string>6.0</string>
              <key>CFBundleName</key>
              <string>$(PRODUCT_NAME)</string>
              <key>CFBundlePackageType</key>
              <string>APPL</string>
              <key>CFBundleShortVersionString</key>
              <string>$(FLUTTER_BUILD_NAME)</string>
              <key>CFBundleSignature</key>
              <string>????</string>
              <key>CFBundleVersion</key>
              <string>$(FLUTTER_BUILD_NUMBER)</string>
              <key>IPHONEOS_DEPLOYMENT_TARGET</key>
              <string>15.0</string>
              <key>UIStatusBarStyle</key>
              <string>UIStatusBarStyleDefault</string>
              <key>UIViewControllerBasedStatusBarAppearance</key>
              <true/>
              <key>NSAppTransportSecurity</key>
              <dict>
                  <key>NSAllowsArbitraryLoads</key>
                  <true/>
              </dict>
              <key>NSPhotoLibraryUsageDescription</key>
              <string>需要访问相册以上传头像</string>
              <key>NSCameraUsageDescription</key>
              <string>需要使用相机拍照上传</string>
              <key>NSMicrophoneUsageDescription</key>
              <string>需要使用麦克风录制语音</string>
              <key>NSLocationWhenInUseUsageDescription</key>
              <string>需要获取位置信息</string>
              <key>NSLocationAlwaysUsageDescription</key>
              <string>需要后台持续获取位置</string>
              <key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
              <string>需要持续获取位置信息</string>
              <key>NSBluetoothAlwaysUsageDescription</key>
              <string>需要蓝牙权限</string>
              <key>NSBluetoothPeripheralUsageDescription</key>
              <string>需要蓝牙外设权限</string>
              <key>UIBackgroundModes</key>
              <array>
                  <string>audio</string>
                  <string>fetch</string>
                  <string>remote-notification</string>
                  <string>processing</string>
                  <string>location</string>
              </array>
              <key>UIFileSharingEnabled</key>
              <false/>
              <key>LSSupportsOpeningDocumentsInPlace</key>
              <false/>
              <key>UISupportedInterfaceOrientations</key>
              <array>
                  <string>UIInterfaceOrientationPortrait</string>
                  <string>UIInterfaceOrientationLandscapeLeft</string>
                  <string>UIInterfaceOrientationLandscapeRight</string>
              </array>
              <key>UIRequiresFullScreen</key>
              <false/>
          </dict>
          </plist>
          PLIST

      # ========== 6. 修改 Bundle ID（仅修改，不破坏结构）==========
      - name: Set Bundle ID
        run: |
          sed -i '' 's/PRODUCT_BUNDLE_IDENTIFIER = [^;]*;/PRODUCT_BUNDLE_IDENTIFIER = com.xsop.forum;/g' ios/Runner.xcodeproj/project.pbxproj
          echo "Bundle ID set."

      # ========== 7. 使用 flutter build ios 构建 .app ==========
      # flutter build ipa 在某些 Flutter 版本中不稳定，改用 build ios + 手动打包
      - name: Build iOS .app
        run: |
          flutter build ios \
            --no-codesign \
            --obfuscate \
            --split-debug-info=build/debug-info \
            --dart-define=BASE_URL=https://xsop.de 2>&1 | tee /tmp/flutter_build.log

          if [ $? -ne 0 ]; then
            echo "=== flutter build ios FAILED ==="
            tail -100 /tmp/flutter_build.log
            exit 1
          fi

          echo "=== flutter build ios SUCCESS ==="

          # 查找生成的 .app
          echo "=== Searching for .app ==="
          find . -name "*.app" -type d 2>/dev/null | grep -v ".symlinks" | head -10

          # 列出 build/ios/ 目录
          echo "=== build/ios/ contents ==="
          ls -laR build/ios/ 2>/dev/null || echo "build/ios/ not found"

          # 列出 ios/build/ 目录
          echo "=== ios/build/ contents ==="
          ls -laR ios/build/ 2>/dev/null || echo "ios/build/ not found"

      # ========== 8. 手动打包 IPA ==========
      - name: Package .app into .ipa
        run: |
          IPA_OUT="${GITHUB_WORKSPACE}/build/ipa_output"
          mkdir -p "$IPA_OUT"

          # 查找 .app 文件（排除 .symlinks 目录）
          APP_PATH=$(find . -name "*.app" -type d -not -path "*/.symlinks/*" 2>/dev/null | head -1)

          if [ -z "$APP_PATH" ]; then
            echo "ERROR: .app not found!"
            echo "=== All .app files ==="
            find . -name "*.app" -type d 2>/dev/null
            exit 1
          fi

          echo "Found .app: $APP_PATH"

          # 获取 App 名称
          APP_NAME=$(basename "$APP_PATH" .app)
          echo "App name: $APP_NAME"

          # 打包为 IPA
          STAGING="$IPA_OUT/staging"
          rm -rf "$STAGING"
          mkdir -p "$STAGING/Payload"
          cp -r "$APP_PATH" "$STAGING/Payload/"

          cd "$STAGING"
          zip -r "${APP_NAME}_TrollStore.ipa" Payload/
          cd "${GITHUB_WORKSPACE}"

          mv "$STAGING/${APP_NAME}_TrollStore.ipa" "$IPA_OUT/"

          echo "=== IPA created ==="
          ls -la "$IPA_OUT/"

          # 验证 IPA 结构
          echo "=== IPA contents ==="
          unzip -l "$IPA_OUT/${APP_NAME}_TrollStore.ipa" | head -20

      # ========== 9. 上传到 Artifacts ==========
      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: TrollStore-IPA
          path: build/ipa_output/*.ipa
          retention-days: 90

      # ========== 10. 上传到 GitHub Releases（打 Tag 时）==========
      - name: Create Release and Upload IPA
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          name: Release ${{ github.ref_name }}
          files: build/ipa_output/*.ipa
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ========== 11. 为手动触发创建 Draft Release ==========
      - name: Create Draft Release for Manual Trigger
        uses: softprops/action-gh-release@v1
        if: github.event_name == 'workflow_dispatch'
        with:
          name: Draft Build ${{ github.run_number }}
          tag_name: v0.0.${{ github.run_number }}
          files: build/ipa_output/*.ipa
          draft: true
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
